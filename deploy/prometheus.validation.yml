global:
  scrape_interval: 5s
  evaluation_interval: 5s
rule_files:
  - /etc/prometheus/alerts.yml
alerting:
  alertmanagers:
    - static_configs:
        - targets: [alertmanager:9093]
scrape_configs:
  - job_name: trip
    static_configs:
      - targets: [backend:9000]
  - job_name: trip-agent
    static_configs:
      - targets: [agent:9000]

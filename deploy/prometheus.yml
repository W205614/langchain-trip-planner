global:
  scrape_interval: 15s
rule_files:
  - /etc/prometheus/alerts.yml
scrape_configs:
  - job_name: trip
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: [backend:9000]
  - job_name: trip-agent
    static_configs:
      - targets: [agent:9000]
